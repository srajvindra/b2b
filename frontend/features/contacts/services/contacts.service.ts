import type { ContactPageType, Contact } from "../types"

export async function fetchContactsByType(_type: ContactPageType): Promise<Contact[]> {
  // Placeholder service – replace with API call or real data source.
  return []
}

